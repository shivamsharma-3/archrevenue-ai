# Workspace Behavioral Guidelines

- **Git Pushing Rule:** Do NOT run `git push` automatically after completing code changes. Keep changes local (or locally committed) and ONLY execute `git push` when the user explicitly instructs to push (e.g. "push", "push it", "push to git").
